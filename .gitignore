old-docs/
classes/

# Claude Code — config local de esta máquina (permisos y dirs personales)
.claude/settings.local.json

# Design System — material fuente de extracción (no versionar; ver ADR-0007)
libs/design-system/sources/
**/*.mhtml

# Design System playground (build local; deps no versionadas)
libs/design-system/node_modules/
libs/design-system/dist/

# apps/api (NestJS) — deps, build y secretos
apps/api/node_modules/
apps/api/dist/
apps/api/coverage/
apps/api/.env

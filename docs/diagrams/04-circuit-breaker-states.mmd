---
title: "Circuit Breaker — quota-service → QR Simple Bolivia"
---
stateDiagram-v2
    [*] --> Closed : Sistema inicia (QR Simple healthy)

    Closed --> Open : ≥ 5 errores consecutivos\no 50% falla en 10s\nllamando a QR Simple API

    Open --> HalfOpen : Timeout 30s expirado\n(auto-recovery probe)

    HalfOpen --> Closed : Request de prueba exitoso\n(QR Simple respondió OK)

    HalfOpen --> Open : Request de prueba falla\n(QR Simple aún caído)

    Open --> Open : Retorna error controlado\n"Pago temporalmente no disponible,\nintente en 30s" [DEGRADED]\np95 < 500ms

    note right of Closed
        Respuesta normal
        p95 < 3s
    end note

    note right of Open
        Fallback: error controlado
        0 cascading failures
        Estudiante ve mensaje de reintento
    end note

    note right of HalfOpen
        1 request de prueba
        Si OK → CLOSED
        Si falla → OPEN 30s más
    end note
